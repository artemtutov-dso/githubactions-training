{
  "name": "sample-workflow-app",
  "version": "1.0.0",
  "description": "Test node dependencies file for GitHub Actions",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist && echo 'console.log(\"Hello from build\");' > dist/index.js",
    "test": "echo 'Error: no test specified' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}